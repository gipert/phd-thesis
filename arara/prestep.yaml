!config
identifier: prestep
name: mkdir
authors:
- Luigi Pertoldi
commands:
- name: Creating build directories
  command: >
    @{
        return getCommand('mkdir', '-p', builddir);
    }
arguments:
    - identifier: builddir
      flag: >
          @{
              return parameters.builddir;
          }
